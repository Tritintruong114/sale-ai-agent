import { ScreenPlaceholder } from "@/components/shared/ScreenPlaceholder";

export default function NudgePage() {
  return (
    <ScreenPlaceholder
      code="—"
      title="Nhắc tự động"
      description="Ngoài luồng demo V0 — giữ trong nhóm Cài đặt."
      willBuild={["Đã có ở prototype cũ; không nằm trong luồng demo V0."]}
    />
  );
}
